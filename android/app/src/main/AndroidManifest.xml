<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- specialUse: foreground service whose long-lived purpose isn't covered
         by Android's specific FGS types (we host an WS connection to user's
         own backend). Requires PROPERTY_SPECIAL_USE_FGS_SUBTYPE below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- QR scanning during enrollment -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Photo access — Android 13+ scoped media -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Battery whitelist prompt redirect (XXPermissions / vendor-specific) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".AgentApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/ic_dialog_info"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgentPlatform"
        tools:targetApi="33"
        xmlns:tools="http://schemas.android.com/tools">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AgentPlatform">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".confirm.ConfirmActivity"
            android:exported="false"
            android:theme="@style/Theme.AgentPlatform"
            android:launchMode="singleTask"
            android:excludeFromRecents="true" />

        <service
            android:name=".service.AgentForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="agent-platform background WebSocket to a user-controlled self-hosted server. Required for instant tool dispatch without push." />
        </service>

    </application>

</manifest>
